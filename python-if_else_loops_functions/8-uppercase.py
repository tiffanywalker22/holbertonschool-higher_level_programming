#!/usr/bin/python3
def uppercase(str):
    new_str = ""
    for letter in str
        if 97 <= ord(c) <= 122:
            uppercase = char(ord(letter) - 32)
            new_str += uppercase
        else:
            new_str += letter
    print(new_str)
